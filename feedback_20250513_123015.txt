URL: https://github.com/Midoirl/DI-BOOTCAMP2025/blob/main/ExDict.py
suggestions for improvement:
- In Exercise 2,  consider adding input validation to handle potential errors if the user enters non-numeric values for ages.  This would improve the robustness of your code.
- In Exercise 3, the string concatenation for printing Zara's clients could be slightly improved for readability. Consider using f-strings or `join()` for better formatting.
- Exercise 3: The bonus section involving merging `more_on_zara` into `brand` isn't explicitly implemented; only the printing of the updated `number_stores` is shown.  The merging operation using the `.update()` method should be shown explicitly for completeness.
Brief justification:
- correctness: The code largely fulfills the requirements of all four exercises.  Exercise 1 correctly converts lists to dictionaries using `zip`. Exercise 2 accurately calculates ticket costs based on age. Exercise 3 successfully manipulates the Zara dictionary, with minor omissions in the bonus. Exercise 4 correctly creates dictionaries based on different character orderings. The minor deductions come from the suggestions mentioned in the feedback section.
- readability: The code is generally readable. Variable names are mostly descriptive. However, some comments could be more specific, explaining the logic behind certain steps more clearly.  The structure of some parts (e.g., Exercise 3) can be improved with more consistent formatting and variable naming.
- performance: The code's performance is efficient for the tasks involved.  The algorithms used (loops, dictionary operations) are appropriate and have a time complexity that scales well with the size of the input data.
- security: No security vulnerabilities are present in the provided code, as it only deals with simple data manipulation and doesn't involve external inputs or file operations that could introduce security risks.

